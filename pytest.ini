[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
markers =
    e2e: end-to-end tests that make real LLM (Groq) and/or web search (Tavily) API calls. Excluded by default due to free-tier rate limits (TPM) - run explicitly with `pytest -m e2e`.
addopts = -m "not e2e"